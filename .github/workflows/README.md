# Workflows

- **unit-test.yaml** — Runs `make test` on every push to master and on pull requests.
- **e2e-test.yaml** — Spins up a kind cluster and runs end-to-end tests on every push to master and on pull requests.
